# ===========================================
# FASHION INVENTORY SYSTEM - ENVIRONMENT CONFIG
# ===========================================

# -----------------
# Database Configuration (PostgreSQL/Neon)
# -----------------
# Replace with your actual database connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL= postgresql://neondb_owner:npg_TVhtKQ2Jnd4Y@ep-hidden-forest-adya5az2-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Individual PostgreSQL credentials (if using separate values)
PGHOST=localhost
PGPORT=5432
PGUSER=fashion_user
PGPASSWORD=your_secure_password
PGDATABASE=fashion_inventory

# -----------------
# Application Configuration
# -----------------
NODE_ENV=development
PORT=5000

# Session secret - IMPORTANT: Generate a secure random string
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_very_long_random_secret_string_change_this_in_production

# -----------------
# Email Configuration (for password reset)
# -----------------
# Gmail Example:
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_specific_password

# SMTP Configuration (alternative to Gmail)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_user
# SMTP_PASSWORD=your_smtp_password

# -----------------
# Twilio Configuration (for WhatsApp notifications)
# -----------------
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# -----------------
# Google Cloud Storage Configuration
# -----------------
# Path to your Google Cloud service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/service-account-key.json

# Google Cloud Storage bucket ID
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your_bucket_name

# Public object search paths (comma-separated)
PUBLIC_OBJECT_SEARCH_PATHS=your_bucket_name/public,your_bucket_name/images

# -----------------
# Authentication Configuration
# -----------------
# Replit Auth (if deploying on Replit)
# REPLIT_AUTH_DOMAIN=your-repl-name.repl.co

# -----------------
# Application Features
# -----------------
# Enable/disable features
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_WHATSAPP_NOTIFICATIONS=false
ENABLE_FILE_UPLOADS=true

# -----------------
# Security Settings
# -----------------
# CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# -----------------
# Logging
# -----------------
LOG_LEVEL=info

# ===========================================
# NOTES:
# 1. NEVER commit this file to version control
# 2. Keep your credentials secure
# 3. Use different values for development and production
# 4. Generate strong random values for SESSION_SECRET
# 5. For Gmail: Use App Passwords (not your regular password)
#    Enable at: https://myaccount.google.com/apppasswords
# ===========================================
